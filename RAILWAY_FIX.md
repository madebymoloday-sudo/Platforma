# 🔧 Исправление проблемы с запуском Frontend на Railway

## Проблема
Frontend показывает ошибку "Application failed to respond" на Railway.

## Что было исправлено:

### 1. Конфигурация Vite Preview
- Обновлен `vite.config.ts` для правильной работы с переменной `PORT`
- Добавлен `host: '0.0.0.0'` для прослушивания всех интерфейсов
- `strictPort: false` для гибкости с портами

### 2. Команда запуска
- Обновлен `nixpacks.toml` для использования `$PORT`
- Обновлен `railway.json` с явным указанием порта
- Команда: `vite preview --host 0.0.0.0 --port $PORT`

### 3. TypeScript проверка
- Изменена команда build: `tsc --noEmit && vite build`
- Это проверяет типы, но не генерирует файлы (Vite сам компилирует)

### 4. Переменные окружения
- Сделаны опциональными в типах TypeScript
- Это позволяет build проходить даже без переменных (они нужны только в runtime)

## 📋 Что нужно сделать на Railway:

### Шаг 1: Перезапустить деплой
1. Откройте ваш Frontend сервис на Railway
2. Перейдите в **Settings** → **Deploy**
3. Нажмите **"Redeploy"** или **"Deploy Latest"**

### Шаг 2: Проверить переменные окружения
Убедитесь, что в **Variables** установлены:
```
VITE_API_URL=https://your-backend-url.railway.app/api
VITE_SOCKET_URL=https://your-backend-url.railway.app
```

**Важно:** Замените `your-backend-url.railway.app` на реальный URL вашего Backend сервиса!

### Шаг 3: Проверить логи
1. Откройте вкладку **Logs** в Frontend сервисе
2. Проверьте, что:
   - Build прошел успешно (зеленая галочка)
   - Сервер запустился на правильном порту
   - Нет ошибок подключения

### Шаг 4: Проверить Backend
Убедитесь, что Backend сервис:
- ✅ Запущен и работает (зеленый индикатор)
- ✅ Доступен по своему URL
- ✅ Имеет правильный `FRONTEND_URL` в переменных окружения

## 🔍 Диагностика проблем:

### Если build не проходит:
1. Проверьте логи в Railway
2. Убедитесь, что все зависимости установлены
3. Проверьте, что TypeScript ошибок нет

### Если сервер не запускается:
1. Проверьте логи - должна быть строка типа:
   ```
   ➜  Local:   http://localhost:5173/
   ➜  Network: http://0.0.0.0:5173/
   ```
2. Убедитесь, что переменная `PORT` установлена Railway автоматически

### Если сайт открывается, но не подключается к API:
1. Проверьте `VITE_API_URL` в переменных окружения
2. Убедитесь, что Backend доступен по указанному URL
3. Проверьте CORS настройки в Backend

## ✅ После исправлений:

1. Закоммитьте изменения:
   ```bash
   git add .
   git commit -m "Fix Railway deployment configuration"
   git push
   ```

2. Railway автоматически пересоберет проект

3. Дождитесь завершения деплоя (зеленая галочка)

4. Откройте URL вашего Frontend сервиса

## 📝 Дополнительные проверки:

### Проверка Backend:
```bash
# В консоли Backend сервиса
curl http://localhost:3001/api/health
# Должен вернуть: {"status":"ok","message":"Platforma API is running"}
```

### Проверка Frontend переменных:
В логах Frontend должны быть видны правильные URL для API.
