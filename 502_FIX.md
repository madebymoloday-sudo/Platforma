# 🔧 Исправление ошибки 502 Bad Gateway

## Проблема
Railway показывает ошибку 502 - это означает, что приложение не запустилось или не слушает правильный порт.

## ✅ Что было исправлено:

1. **Добавлен `serve`** - более надежный статический сервер для production
2. **Команда запуска** теперь использует `serve -s dist -l $PORT`
3. **Все конфигурационные файлы** обновлены

## 🚀 Что делать СЕЙЧАС:

### 1. Закоммитьте изменения:

```bash
cd /Users/pavelgulo/Desktop/Platformа
git add .
git commit -m "Fix 502 error: use serve for static files"
git push
```

### 2. На Railway:

1. Откройте **Frontend** сервис
2. Дождитесь автоматического деплоя (или нажмите **Redeploy**)
3. Проверьте **Logs**

### 3. Проверьте логи:

**Что должно быть в логах:**

```
✓ built in Xs
```

После build должна быть строка:
```
INFO: Accepting connections at http://[::]:XXXX
```

Или:
```
Serving!
```

### 4. Если всё ещё ошибка 502:

#### Проверьте, что build прошёл успешно:
- В логах должна быть папка `dist` с файлами
- Не должно быть ошибок TypeScript

#### Проверьте переменные окружения:
- `VITE_API_URL` должен быть установлен
- `VITE_SOCKET_URL` должен быть установлен

#### Проверьте, что dist папка создана:
В логах build должна быть строка:
```
dist/index.html
dist/assets/...
```

## 🔍 Альтернативное решение (если serve не работает):

Если `serve` не работает, можно вернуться к `vite preview`, но с правильной настройкой:

1. Обновите `vite.config.ts`:
```typescript
preview: {
  port: Number(process.env.PORT) || 5173,
  host: '0.0.0.0',
  strictPort: true  // Важно!
}
```

2. Обновите команду запуска:
```bash
vite preview --host 0.0.0.0 --port $PORT
```

## 📋 Чеклист:

- [ ] Код закоммичен и запушен
- [ ] Railway пересобрал проект
- [ ] Build прошёл успешно (зелёная галочка)
- [ ] В логах видно, что сервер запустился
- [ ] Нет ошибок в логах
- [ ] Переменные окружения установлены

## 💡 Если проблема не решается:

1. **Проверьте полные логи** в Railway (Frontend → Logs)
2. **Скопируйте последние 50 строк логов** и проверьте на ошибки
3. **Убедитесь, что build прошёл** - должна быть папка `dist`
4. **Попробуйте пересоздать сервис** (если ничего не помогает)
