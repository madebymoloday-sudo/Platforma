# 🚂 Инструкция по развертыванию на Railway

## Шаг 1: Подготовка GitHub репозитория

Код уже загружен на GitHub. Убедитесь, что репозиторий доступен.

## Шаг 2: Создание сервисов на Railway

### 2.1. Создание PostgreSQL базы данных

1. Войдите в [Railway](https://railway.app)
2. Нажмите "New Project"
3. Выберите "Provision PostgreSQL"
4. Railway автоматически создаст базу данных
5. **Важно:** Скопируйте `DATABASE_URL` из вкладки "Variables"
   - Формат: `postgresql://user:password@host:port/railway`

### 2.2. Создание Backend сервиса

1. В том же проекте нажмите "New Service"
2. Выберите "Deploy from GitHub repo"
3. Выберите ваш репозиторий
4. Railway автоматически определит проект

**Настройки:**
- **Root Directory:** `backend`
- **Build Command:** `npm run build` (автоматически)
- **Start Command:** `npm start` (автоматически)

**Переменные окружения (Variables):**
```
DATABASE_URL=<скопируйте из PostgreSQL сервиса>
JWT_SECRET=<сгенерируйте случайную строку, например через: openssl rand -hex 32>
PORT=3001
NODE_ENV=production
OPENAI_API_KEY=<ваш OpenAI API ключ>
FRONTEND_URL=<URL вашего frontend сервиса, например: https://your-frontend.railway.app>
```

**После первого деплоя:**
1. Откройте консоль сервиса (View Logs → Open Shell)
2. Выполните миграции:
   ```bash
   npx prisma migrate deploy
   ```

### 2.3. Создание Frontend сервиса

1. В том же проекте нажмите "New Service"
2. Выберите "Deploy from GitHub repo"
3. Выберите тот же репозиторий

**Настройки:**
- **Root Directory:** `frontend`
- **Build Command:** `npm run build` (автоматически)
- **Start Command:** `npm run preview` (автоматически)

**Переменные окружения (Variables):**
```
VITE_API_URL=<URL вашего backend сервиса, например: https://your-backend.railway.app/api>
VITE_SOCKET_URL=<URL вашего backend сервиса без /api, например: https://your-backend.railway.app>
```

**Важно:** После добавления переменных окружения, Railway автоматически пересоберёт проект.

## Шаг 3: Настройка доменов

### Backend:
1. В настройках Backend сервиса → Settings → Generate Domain
2. Скопируйте URL (например: `your-backend.railway.app`)

### Frontend:
1. В настройках Frontend сервиса → Settings → Generate Domain
2. Скопируйте URL (например: `your-frontend.railway.app`)

### Обновление переменных окружения:

**Backend:**
- Обновите `FRONTEND_URL` на URL вашего frontend

**Frontend:**
- Обновите `VITE_API_URL` на `https://your-backend.railway.app/api`
- Обновите `VITE_SOCKET_URL` на `https://your-backend.railway.app`

Railway автоматически пересоберёт проекты при изменении переменных.

## Шаг 4: Проверка работы

1. Откройте URL вашего Frontend сервиса
2. Зарегистрируйте первого пользователя
3. Проверьте все функции

## 🔧 Решение проблем

### Backend не запускается:
- Проверьте логи в Railway
- Убедитесь, что `DATABASE_URL` правильный
- Проверьте, что миграции выполнены: `npx prisma migrate deploy`

### Frontend не подключается к Backend:
- Проверьте `VITE_API_URL` и `VITE_SOCKET_URL`
- Убедитесь, что CORS настроен правильно в backend
- Проверьте, что backend доступен по URL

### База данных:
- Убедитесь, что PostgreSQL сервис запущен
- Проверьте `DATABASE_URL` формат
- Выполните миграции вручную через консоль

## 📝 Дополнительные команды

### Выполнить миграции вручную:
```bash
# В консоли Backend сервиса
npx prisma migrate deploy
npx prisma generate
```

### Просмотр базы данных:
```bash
# В консоли Backend сервиса
npx prisma studio
# Откроется локально, но подключится к Railway БД
```

## 🎉 Готово!

После выполнения всех шагов ваша платформа будет доступна по URL вашего Frontend сервиса.
