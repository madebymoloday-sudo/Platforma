# 🔧 Ручное исправление команды запуска на Railway

## Проблема
Railway всё ещё использует `npm run preview` вместо `npm start`, и vite preview не использует PORT от Railway.

## ✅ Решение 1: Исправить через веб-интерфейс Railway

1. Откройте ваш **Frontend** сервис на Railway
2. Перейдите в **Settings** → **Deploy**
3. Найдите поле **"Start Command"** (или **"Start Command"** в разделе Deploy)
4. Измените команду на:
   ```
   npm start
   ```
   Или напрямую:
   ```
   serve -s dist -l $PORT
   ```
5. Сохраните изменения
6. Railway автоматически пересоберёт проект

## ✅ Решение 2: Использовать переменную окружения

Если Railway всё ещё использует `npm run preview`, можно добавить переменную окружения:

1. Откройте **Frontend** сервис → **Variables**
2. Добавьте переменную:
   ```
   NPM_CONFIG_START_COMMAND=npm start
   ```
3. Или измените команду preview в package.json (уже сделано)

## ✅ Решение 3: Проверить Root Directory

Убедитесь, что:
1. **Root Directory** установлен в `frontend`
2. Railway видит правильный `package.json`

## 📋 Что должно быть в логах после исправления:

**Если используется `serve`:**
```
INFO: Accepting connections at http://[::]:XXXX
```

**Если используется `vite preview`:**
```
➜  Network: http://0.0.0.0:XXXX/
```

Где `XXXX` - это порт, назначенный Railway (не 5173!).

## 🔍 Проверка

После исправления проверьте:
1. Логи показывают правильный порт (не 5173)
2. Сервер запустился успешно
3. Сайт открывается без ошибки 502
