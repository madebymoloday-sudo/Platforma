# 🔧 Принудительное обновление Railway

## Проблема
Railway показывает старую команду `npm run preview` вместо `npx serve -s dist -l $PORT` из `railway.json`.

## ✅ Проверка на GitHub

Файл, который Railway должен читать:
**`frontend/railway.json`**

На GitHub этот файл содержит:
```json
{
  "deploy": {
    "startCommand": "npx serve -s dist -l $PORT"
  }
}
```

## 🔍 Что делать:

### Вариант 1: Проверить на GitHub напрямую
1. Откройте: `https://github.com/madebymoloday-sudo/Platforma/blob/main/frontend/railway.json`
2. Убедитесь, что строка 8 содержит: `"startCommand": "npx serve -s dist -l $PORT"`
3. Если там старая версия - значит изменения не запушены

### Вариант 2: Принудительно обновить Railway
1. На Railway откройте **Frontend** сервис
2. Перейдите в **Settings** → **Source**
3. Попробуйте **"Disconnect"** и заново **"Connect"** репозиторий
4. Или нажмите **"Redeploy"** с последнего коммита

### Вариант 3: Создать новый деплой
1. Откройте **Frontend** → **Deployments**
2. Найдите последний коммит `ab9a4e3` (Fix: use npx serve...)
3. Нажмите на три точки → **"Redeploy"**

### Вариант 4: Временное решение через Pre-deploy
Если ничего не помогает, можно использовать Pre-deploy Command:
1. В настройках Railway в поле **"Pre-deploy Command"** укажите:
   ```
   npm start
   ```
2. А в **"Custom Start Command"** оставьте пустым или укажите то же самое

## 📋 Файлы, которые Railway читает (в порядке приоритета):

1. **`frontend/railway.json`** - основной файл конфигурации
2. **`frontend/nixpacks.toml`** - если есть (мы его удалили)
3. **`frontend/package.json`** - для автоматического определения команды

## ⚠️ Важно:

Railway может кэшировать конфигурацию. Если файл на GitHub правильный, но Railway показывает старое значение:
- Подождите 1-2 минуты (кэш может обновиться)
- Или пересоздайте сервис
- Или отключите и заново подключите репозиторий
