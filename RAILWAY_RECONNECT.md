# 🔄 Переподключение Frontend на Railway

## Проблема
Railway использует старый коммит `bc2ac5a` и не обновляется на новые коммиты.

## ✅ Решение: Переподключить репозиторий

### Вариант 1: Переподключить существующий сервис

1. Откройте **Frontend** сервис на Railway
2. Перейдите в **Settings** → **Source**
3. Нажмите **"Disconnect"** (отключить репозиторий)
4. Затем **"Connect GitHub"** и выберите репозиторий заново
5. Убедитесь, что выбрана ветка **`main`**
6. Railway автоматически начнёт новый деплой с последнего коммита

### Вариант 2: Создать новый Frontend сервис

Если переподключение не помогает:

1. **Удалите старый Frontend сервис:**
   - Frontend → Settings → Danger Zone → Delete Service

2. **Создайте новый Frontend сервис:**
   - В проекте нажмите **"New Service"**
   - Выберите **"Deploy from GitHub repo"**
   - Выберите репозиторий `madebymoloday-sudo/Platforma`
   - **Root Directory:** `frontend`
   - Railway автоматически определит конфигурацию

3. **Настройте переменные окружения:**
   ```
   VITE_API_URL=https://ваш-backend-url.railway.app/api
   VITE_SOCKET_URL=https://ваш-backend-url.railway.app
   ```

4. **Проверьте команду запуска:**
   - Settings → Deploy → Custom Start Command
   - Должно быть: `npx serve -s dist -l $PORT`
   - Если нет - укажите вручную

### Вариант 3: Принудительный деплой с конкретного коммита

1. Откройте **Frontend** → **Deployments**
2. Нажмите **"New Deployment"** или **"Redeploy"**
3. Выберите коммит **`fed3048`** (последний с правильной конфигурацией)
4. Railway соберёт проект с этого коммита

## 📋 Проверка после переподключения

После любого из вариантов проверьте:

1. **Логи деплоя:**
   - Должна быть строка: `INFO: Accepting connections at http://[::]:XXXX`
   - Где `XXXX` - порт от Railway

2. **Настройки:**
   - Settings → Deploy → Custom Start Command
   - Должно быть: `npx serve -s dist -l $PORT`

3. **Переменные окружения:**
   - `VITE_API_URL` и `VITE_SOCKET_URL` должны быть установлены

## ⚠️ Важно

Если создаёте новый сервис, не забудьте:
- Обновить `FRONTEND_URL` в Backend сервисе
- Обновить домен в настройках Networking
