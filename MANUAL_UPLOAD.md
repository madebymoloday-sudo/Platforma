# 📤 Ручная загрузка кода на GitHub

## Проблема
Git push не работает из-за ограничений доступа токена (403 ошибка).

## Решение: Загрузка через веб-интерфейс GitHub

### Вариант 1: Загрузка через веб-интерфейс (самый простой)

1. **Откройте репозиторий:**
   - https://github.com/madebymoloday-sudo/Platforma

2. **Если репозиторий пустой, вы увидите инструкцию:**
   - Нажмите на кнопку **"uploading an existing file"** или **"upload files"**

3. **Загрузите файлы:**
   - Перетащите всю папку `Platformа` в окно браузера
   - Или выберите файлы через "choose your files"
   - Добавьте commit message: "Initial commit: Platformа - платформа обучения и сообщества"
   - Нажмите **"Commit changes"**

### Вариант 2: Использовать GitHub Desktop

1. **Скачайте GitHub Desktop:**
   - https://desktop.github.com/

2. **Настройте:**
   - File → Add Local Repository
   - Выберите: `~/Desktop/Platformа`
   - Нажмите "Publish repository"
   - Выберите: `madebymoloday-sudo/Platforma`

3. **Загрузите:**
   - Нажмите "Publish repository"

### Вариант 3: Использовать SSH (если настроен)

1. **Проверьте SSH ключ:**
   ```bash
   ls -la ~/.ssh/id_*.pub
   ```

2. **Если нет ключа, создайте:**
   ```bash
   ssh-keygen -t ed25519 -C "your_email@example.com"
   ```

3. **Добавьте ключ на GitHub:**
   - Скопируйте: `cat ~/.ssh/id_ed25519.pub`
   - Добавьте на: https://github.com/settings/keys

4. **Измените remote:**
   ```bash
   cd ~/Desktop/Platformа
   git remote set-url origin git@github.com:madebymoloday-sudo/Platforma.git
   git push -u origin main
   ```

### Вариант 4: Создать архив и загрузить

1. **Создайте архив:**
   ```bash
   cd ~/Desktop
   tar -czf Platforma.tar.gz Platformа
   ```

2. **Загрузите через веб-интерфейс:**
   - Распакуйте архив
   - Загрузите файлы как в Варианте 1

## После загрузки

После успешной загрузки кода на GitHub:

1. **Проверьте репозиторий:**
   - https://github.com/madebymoloday-sudo/Platforma
   - Убедитесь, что все файлы загружены

2. **Разверните на Railway:**
   - Следуйте инструкции в `RAILWAY_DEPLOY.md`
   - Подключите репозиторий: `madebymoloday-sudo/Platforma`

## Текущий статус

- ✅ Токен сохранён: `~/.platforma-secrets/github-token.txt`
- ✅ Git репозиторий настроен
- ✅ Все файлы готовы к загрузке
- ⚠️ Git push не работает (403 ошибка)
- ✅ Репозиторий существует и доступен

## Структура проекта для загрузки

```
Platformа/
├── backend/
│   ├── src/
│   ├── prisma/
│   ├── uploads/
│   ├── package.json
│   └── ...
├── frontend/
│   ├── src/
│   ├── public/
│   ├── package.json
│   └── ...
├── .gitignore
├── README.md
├── RAILWAY_DEPLOY.md
└── ...
```

**Важно:** Не загружайте файлы из папки `~/.platforma-secrets/` (токены и секреты)!
