# 🚂 Railway Setup - Пошаговая инструкция

## ✅ Что уже сделано:
- ✅ Backend сервис добавлен (Root: `backend`)
- ✅ Frontend сервис добавлен (Root: `frontend`)
- ✅ PostgreSQL база данных добавлена

---

## 📋 ШАГ 1: Найти DATABASE_URL

1. В Railway найдите сервис **PostgreSQL** (база данных)
2. Откройте вкладку **"Variables"** (или **"Data"** → **"Connect"**)
3. Найдите переменную **`DATABASE_URL`**
4. Скопируйте её полностью (она выглядит примерно так: `postgresql://postgres:password@host:port/railway`)

---

## 📋 ШАГ 2: Настроить Backend

1. Откройте сервис **Backend** в Railway
2. Перейдите в **"Variables"** (вкладка вверху)
3. Нажмите **"+ New Variable"** и добавьте следующие переменные:

### Переменные для Backend:

```
DATABASE_URL=<вставьте скопированный DATABASE_URL из PostgreSQL>
```

```
JWT_SECRET=6b5085b347af39ebd1d7ccdff45faa7051ca5ba07aeb747d4cc928dad0e30698
```

```
PORT=3001
```

```
NODE_ENV=production
```

```
FRONTEND_URL=<пока оставьте пустым, добавите после Шага 4>
```

```
OPENAI_API_KEY=<ваш ключ OpenAI, если есть (опционально)>
```

4. После добавления всех переменных, Railway автоматически пересоберёт backend

---

## 📋 ШАГ 3: Выполнить миграции базы данных

1. В сервисе **Backend** откройте вкладку **"Deployments"**
2. Дождитесь завершения деплоя (зелёная галочка)
3. Откройте вкладку **"Logs"**
4. Нажмите кнопку **"Open Shell"** (или **"Terminal"**)
5. В открывшейся консоли выполните:

```bash
npx prisma migrate deploy
```

6. Дождитесь сообщения "All migrations have been successfully applied"

---

## 📋 ШАГ 4: Настроить домены

### 4.1. Backend домен:

1. В сервисе **Backend** откройте вкладку **"Settings"**
2. Прокрутите до раздела **"Networking"**
3. Нажмите **"Generate Domain"**
4. Скопируйте URL (например: `platforma-backend-production.up.railway.app`)

### 4.2. Frontend домен:

1. В сервисе **Frontend** откройте вкладку **"Settings"**
2. Прокрутите до раздела **"Networking"**
3. Нажмите **"Generate Domain"**
4. Скопируйте URL (например: `platforma-frontend-production.up.railway.app`)

---

## 📋 ШАГ 5: Обновить переменные окружения

### 5.1. Обновить Backend:

1. В сервисе **Backend** → **"Variables"**
2. Найдите переменную **`FRONTEND_URL`**
3. Измените её значение на URL вашего Frontend:
   ```
   FRONTEND_URL=https://frontend-production-559d.up.railway.app
   ```

### 5.2. Настроить Frontend:

1. В сервисе **Frontend** → **"Variables"**
2. Нажмите **"+ New Variable"** и добавьте:

```
VITE_API_URL=https://platforma-backend-production.up.railway.app/api
```
(замените на ваш реальный Backend URL + `/api`)

```
VITE_SOCKET_URL=https://platforma-backend-production.up.railway.app
```
(замените на ваш реальный Backend URL без `/api`)

---

## 📋 ШАГ 6: Проверка

1. Дождитесь завершения деплоя обоих сервисов (зелёные галочки)
2. Откройте URL вашего **Frontend** сервиса в браузере
3. Зарегистрируйте первого пользователя
4. Проверьте работу всех функций

---

## 🔧 Решение проблем

### Backend не запускается:
- Проверьте логи в **Backend** → **"Logs"**
- Убедитесь, что `DATABASE_URL` правильный
- Проверьте, что миграции выполнены (Шаг 3)

### Frontend показывает ошибки подключения:
- Проверьте `VITE_API_URL` и `VITE_SOCKET_URL` в Frontend Variables
- Убедитесь, что Backend доступен по своему URL
- Проверьте логи Frontend → **"Logs"**

### База данных:
- Убедитесь, что PostgreSQL сервис запущен (зелёный индикатор)
- Проверьте формат `DATABASE_URL`
- Выполните миграции вручную через Shell (Шаг 3)

---

## 📝 Полезные команды

### Просмотр базы данных:
```bash
# В Shell Backend сервиса
npx prisma studio
```

### Проверка подключения к БД:
```bash
# В Shell Backend сервиса
npx prisma db pull
```

---

## ✅ Готово!

После выполнения всех шагов ваша платформа будет доступна по URL Frontend сервиса.

**Следующие шаги:**
- Откройте Frontend URL
- Зарегистрируйтесь
- Начните использовать платформу!
