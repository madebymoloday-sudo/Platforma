# 🚀 Итоговая инструкция по развертыванию

## ✅ Что уже готово:

1. ✅ Код подготовлен для развертывания
2. ✅ Настроены переменные окружения
3. ✅ Созданы конфигурации для Railway
4. ✅ Обновлены все URL для production

## 📋 Пошаговая инструкция:

### 1. Загрузите код на GitHub

Следуйте инструкции в файле `GITHUB_UPLOAD.md`

### 2. Разверните на Railway

Следуйте подробной инструкции в файле `RAILWAY_DEPLOY.md`

### Краткая версия:

#### Шаг 1: PostgreSQL
- Создайте сервис PostgreSQL
- Скопируйте `DATABASE_URL`

#### Шаг 2: Backend
- Создайте сервис из GitHub
- Root Directory: `backend`
- Переменные:
  - `DATABASE_URL` (из PostgreSQL)
  - `JWT_SECRET` (случайная строка)
  - `PORT=3001`
  - `NODE_ENV=production`
  - `OPENAI_API_KEY` (ваш ключ)
  - `FRONTEND_URL` (URL frontend после создания)

#### Шаг 3: Frontend
- Создайте сервис из GitHub
- Root Directory: `frontend`
- Переменные:
  - `VITE_API_URL` (URL backend + /api)
  - `VITE_SOCKET_URL` (URL backend без /api)

#### Шаг 4: Миграции
- В консоли Backend выполните:
  ```bash
  npx prisma migrate deploy
  ```

## 🎯 Важные моменты:

1. **Порядок создания:** PostgreSQL → Backend → Frontend
2. **Переменные окружения:** Обновляйте после создания каждого сервиса
3. **Миграции:** Обязательно выполните после первого деплоя backend
4. **CORS:** Настроен автоматически через `FRONTEND_URL`

## 🔗 Полезные ссылки:

- [Railway Dashboard](https://railway.app/dashboard)
- [Railway Docs](https://docs.railway.app)

## 📞 Если что-то не работает:

1. Проверьте логи в Railway
2. Убедитесь, что все переменные окружения установлены
3. Проверьте, что миграции выполнены
4. Убедитесь, что сервисы запущены (Status = Running)

