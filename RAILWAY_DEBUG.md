# 🔍 Диагностика проблемы с Railway

## Шаг 1: Проверьте логи Frontend

В Railway откройте **Frontend сервис** → **Logs** и проверьте:

### ✅ Что должно быть в логах:

**При успешном build:**
```
✓ built in Xs
```

**При успешном запуске:**
```
➜  Local:   http://localhost:XXXX/
➜  Network: http://0.0.0.0:XXXX/
```

### ❌ Возможные ошибки:

1. **"Cannot find module"** или **"Module not found"**
   - Проблема с зависимостями
   - Решение: проверьте, что `npm ci` выполнился успешно

2. **"Port XXXX is already in use"**
   - Порт занят
   - Решение: Railway должен автоматически назначить порт

3. **"dist directory not found"**
   - Build не выполнился
   - Решение: проверьте логи build фазы

4. **"Application failed to respond"**
   - Сервер не запустился
   - Решение: проверьте команду запуска

## Шаг 2: Проверьте логи Backend

В Railway откройте **Backend сервис** → **Logs** и проверьте:

### ✅ Что должно быть:
```
🚀 Server running on http://localhost:3001
📊 Environment: production
```

### ❌ Возможные ошибки:

1. **"Database connection error"**
   - Проблема с `DATABASE_URL`
   - Решение: проверьте переменную окружения

2. **"Migration failed"**
   - Проблема с миграциями
   - Решение: выполните `npx prisma migrate deploy` вручную

3. **"Port 3001 is already in use"**
   - Порт занят
   - Решение: Railway должен автоматически назначить порт

## Шаг 3: Проверьте переменные окружения

### Frontend:
```
VITE_API_URL=https://your-backend.railway.app/api
VITE_SOCKET_URL=https://your-backend.railway.app
```

**Проверьте:**
- ✅ URL начинается с `https://`
- ✅ `VITE_API_URL` заканчивается на `/api`
- ✅ `VITE_SOCKET_URL` НЕ заканчивается на `/api`
- ✅ URL указывают на правильный Backend сервис

### Backend:
```
DATABASE_URL=postgresql://...
JWT_SECRET=...
PORT=3001
NODE_ENV=production
FRONTEND_URL=https://your-frontend.railway.app
```

**Проверьте:**
- ✅ `DATABASE_URL` правильный (скопирован из PostgreSQL сервиса)
- ✅ `FRONTEND_URL` указывает на Frontend сервис
- ✅ `FRONTEND_URL` начинается с `https://`

## Шаг 4: Проверьте статус сервисов

Все три сервиса должны быть:
- ✅ **Зелёные** (Running)
- ✅ **Не красные** (Failed)
- ✅ **Не жёлтые** (Deploying)

## Шаг 5: Проверьте домены

1. Откройте **Frontend** → **Settings** → **Networking**
2. Убедитесь, что домен сгенерирован
3. Скопируйте URL и проверьте в браузере

4. Откройте **Backend** → **Settings** → **Networking**
5. Убедитесь, что домен сгенерирован
6. Проверьте доступность: `https://your-backend.railway.app/api/health`

## Шаг 6: Тест API

Откройте в браузере или через curl:
```
https://your-backend.railway.app/api/health
```

Должен вернуться:
```json
{"status":"ok","message":"Platforma API is running"}
```

Если не работает:
- Проверьте логи Backend
- Проверьте переменные окружения
- Проверьте, что сервис запущен

## Шаг 7: Проверьте консоль браузера

Откройте Frontend URL и:
1. Нажмите F12 (открыть DevTools)
2. Перейдите на вкладку **Console**
3. Проверьте ошибки

Возможные ошибки:
- **"Failed to fetch"** - проблема с `VITE_API_URL`
- **"CORS error"** - проблема с `FRONTEND_URL` в Backend
- **"Network error"** - Backend недоступен

## 🔧 Быстрые исправления

### Если Frontend не запускается:

1. **Проверьте build:**
   ```bash
   # В консоли Frontend сервиса (Railway Shell)
   ls -la dist/
   ```
   Должна быть папка `dist` с файлами

2. **Попробуйте запустить вручную:**
   ```bash
   vite preview --host 0.0.0.0 --port $PORT
   ```

### Если Backend не запускается:

1. **Проверьте миграции:**
   ```bash
   # В консоли Backend сервиса
   npx prisma migrate deploy
   npx prisma generate
   ```

2. **Проверьте подключение к БД:**
   ```bash
   npx prisma db pull
   ```

## 📝 Что делать дальше

Если проблема не решена:
1. Скопируйте **полные логи** из Railway (и Frontend, и Backend)
2. Проверьте все переменные окружения
3. Убедитесь, что код закоммичен и запушен на GitHub
4. Попробуйте пересоздать сервисы (если ничего не помогает)
